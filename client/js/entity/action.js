define(function(){
    var Action = Class.extend({
        init(id,source,targets){
            this.id      = msg
            this.source  = source
            this.targets = targets
            this.ignore  = false
            this.result  = "error" 
        }

    })
    return Action
})