define(["animation/effect"],function(Effect){
    var Effects = {
        ZoomInEffect : Effect.extend({}),
        ZoomOutEffect: Effect.extend({}),
        AttackEffect : Effect.extend({}),
        SummonEffect : Effect.extend({})
    }
    return Effect;
})